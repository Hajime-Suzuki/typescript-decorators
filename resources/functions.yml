main:
  handler: src/decorator-example/main.handler
  events:
    - http:
        path: /decorator/{id}
        method: post

main-functional:
  handler: src/functional-example/main.handler
  events:
    - http:
        path: /pipe/{id}
        method: post
